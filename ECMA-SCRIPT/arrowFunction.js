// simple function

function info1(fname, lname) {
  return `${fname} ${lname}}`;
}

console.log(info1('Shikshya', 'Gautam'));

//Arrow function
const info = (fname, lname) => {
  return `${fname} ${lname}}`;
};

console.log(info('Shikshya', 'Gautam'));
