// When we use var , let we can stire only one value at a time
/*
let friend1 = 'JOhn';
let friend2 = 'Mary';
let friend3 = 'Danny';
*/

// When we feel like storing multiple values in one variable then instead of var we use array
// let myFriends = ['John', 'Mary', 'Daniel'];

// In js we have array class and arrays are the prototype of  this class

let myInfo = ['Shikshya', 22, ['coding', 'sleeping'], true];

console.log(myInfo.length); // length of an array
console.log(myInfo[3]); // find element at given index
console.log(myInfo[myInfo.length - 1]); // find last element of the array
